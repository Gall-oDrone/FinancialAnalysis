apiVersion: v1
kind: ServiceAccount
metadata:
  name: financial-analysis-sa
  namespace: financial-analysis
  labels:
    app.kubernetes.io/name: financial-analysis
  annotations:
    # This annotation will be overridden per environment
    eks.amazonaws.com/role-arn: ${APPLICATION_ROLE_ARN}

